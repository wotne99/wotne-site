// main.jsx
import React from "react";
import ReactDOM from "react-dom/client";
import Wotne from "./Wotne"; // Wotne bileşenini içeri aktarıyoruz

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Wotne />
  </React.StrictMode>
);
